# Example scan-session schedule for bst recording
#
# Run this example from a shell like this:
# bash$ ilisa_obs bst_ScnSes.yaml

scans:
  - beam:
      freqspec: '52e6:148e6'
      pointing: Cas-A
    rec: [bst]
    integration:  1
    duration: 60
